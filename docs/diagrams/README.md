# Diagrams

Most diagrams are embedded directly as Mermaid code blocks inside the relevant doc (e.g. `architecture.md`). Use this folder only for exported images (PNG/SVG) when a diagram needs to be authored outside Mermaid.

Nothing here yet.
