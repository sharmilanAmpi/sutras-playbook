# Deployment

Where Sutras runs, and how a change gets from a merged PR to production.

## Environments

> TODO: table of environments (local / staging / production), cloud provider per environment, and what's different about each.

## Production infrastructure

> TODO: compute (cluster/hosting), database, caching, ingress/DNS/CDN.

## Release process

> TODO: how a release is triggered, what CI/CD does, rollout mechanism (e.g. Helm upgrade), rollback path.

## Secrets & configuration

> TODO: how secrets are managed per environment (no actual secret values here — just the mechanism).
